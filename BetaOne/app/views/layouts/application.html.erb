<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Proto" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Proto" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag 'vendor/modernizr' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <script src="http://code.jquery.com/jquery-1.11.0.min.js"></script>
    <script src="http://code.jquery.com/jquery-migrate-1.2.1.min.js"></script>
   
   <script src="http://igorescobar.github.io/jQuery-Mask-Plugin/js/jquery.mask.min.js"></script>
  </head>

  <body>
    <header>
      <%= render 'layouts/topBar' %>
    </header>
    <%= render 'layouts/navigation', arr_url_nav: ApplicationHelper::nav_item(session[:user_id]) %>
    <section id="ContentContainer">
      <section class="ContentWrapper">
        <%= render 'layouts/submenu', submenu_for: request.fullpath  %>
        <%= yield %>
      </section>
    </section>
    
    <footer>
       <%= render 'layouts/footer' %>
    </footer> 

  </body>
</html>
