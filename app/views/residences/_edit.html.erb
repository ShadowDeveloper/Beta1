<%= form_for @residence, :html => {method: "PUT", :multipart => true } do |form| %>

	<div class="form-group">
		<%= form.file_field :pics %>
	</div>

  <div class="form-group">
  	 <%= form.submit "Cadastrar imagem", :class => "btn btn-success col-lg-4 pull-right" %>
  </div>
  
<% end %>